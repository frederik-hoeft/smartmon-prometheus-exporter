services:
  smartctl-exporter:
    build:
      context: ./SmartmonExporter
    restart: no
    volumes:
      - ./conf/settings.json:/app/settings.json:ro
      - ./data:/data
    command: /app/SmartmonExporter export
